# Build helpers

- `merge_bin.py` — merge PlatformIO artifacts into one flashable image for ESP Web Tools.
- `prepare_build.py` — (Phase 2) drop the generated `config.ini` into a Marlin checkout and select the PlatformIO env.
